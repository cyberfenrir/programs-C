#include<iostream.h>
#include<conio.h>
#include<math.h>
void main()
{
   clrscr();
   int n,a;
   cout<<"Enter the number ofterms:";
   cin>>n;
   cout<<"Enter the number:";
   cin>>a;
   double sum = 0,div;
   int i;
   double j;
   for(i=1; i<=n; i++)
   {
      j = pow(2,i);
      div = a/j;
      sum = sum+div;
   }
   cout<<sum;
   getch();
}