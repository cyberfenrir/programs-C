#include<iostream.h>
#include<conio.h>
#include<math.h>
void main()
{
	clrscr();
	double x;
	double y;
	cout<<"Enter first number: ";
	cin>>x;
	cout<<"Enter second number: ";
	cin>>y;
	double b = ((3*x)+(5*y))/((5*x)+(3*y));
	double c =(8*x*y)/(2*x*y);
	double a = b-c;
	double w = 3.0/2.0;
	double e = pow(a,w);
	cout<<"the value of the expression is: "<<e;
	getch();
}